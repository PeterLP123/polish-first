# Reviewed Polish recordings

Only consented, human-recorded files that pass `docs/content-qa.md` belong here. Keep masters elsewhere; commit compact web exports named `<phrase-id>.mp3` and register every file in `src/data/audio-manifest.js`.

